# SLICES template

Agent-facing delivery plan derived from an accepted or coherent SPEC. Write in English under `.goals/<goal-slug>/SLICES.md`. Explain the batch to the human in plain language; do not treat this file as the human interface.

## Execution plan

- Dependency / conflict graph summary:
- Parallel waves:
- Integration order:
- Resource and isolation plan:
- Proposed concurrency limit:
- Goal-validation disposition: `Required` / `Per-slice evidence sufficient` (rationale):

## Slices

### Slice `<id>`

- Outcome:
- In scope / out of scope:
- Protected behavior:
- SPEC decisions realized:
- Dependencies and required inputs:
- Affected surfaces / overlap:
- Validation-table gate IDs:
- Product Validation: gate IDs or `Not applicable` (rationale):
- Workspace and resource needs / cleanup owner:
- Commit boundary:
