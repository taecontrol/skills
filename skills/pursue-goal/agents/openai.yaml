interface:
  display_name: "Pursue Goal"
  short_description: "Design first, then deliver isolated slices"
  default_prompt: "Use $pursue-goal to complete and accept the collaborative design and full slice batch before dispatching isolated end-to-end slice delivery, then apply the accepted goal-validation disposition."
policy:
  allow_implicit_invocation: false
