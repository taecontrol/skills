interface:
  display_name: "Pursue Goal"
  short_description: "Design first, then one checkpoint per session"
  default_prompt: "Use $pursue-goal to define this objective with me and pursue one accepted checkpoint per session."
policy:
  allow_implicit_invocation: false
