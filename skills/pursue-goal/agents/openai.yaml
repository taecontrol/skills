interface:
  display_name: "Pursue Goal"
  short_description: "Route discovery and deliver verified vertical slices"
  default_prompt: "Use $pursue-goal to coordinate this objective from the current uncertainty frontier, dispatch the needed capabilities, and deliver each accepted vertical slice through verified local commit."
policy:
  allow_implicit_invocation: false
