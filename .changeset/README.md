# Changesets

Every user-visible catalog change must include a changeset. Run `npm run changeset`, choose `taecontrol-skills`, select the SemVer impact, and explain the outcome for skill users.

For a retirement or rename, name the replacement or say explicitly that no replacement exists.
